info:
  name: Get My Analytics
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/analytics/me"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get analytics for my profile
