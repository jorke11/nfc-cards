info:
  name: Check Username Availability
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/profiles/check-username/jorge-pinedo"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Check if username is available
