info:
  name: Get Profile by Unique ID
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/profiles/{{uniqueId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get public profile by unique ID
