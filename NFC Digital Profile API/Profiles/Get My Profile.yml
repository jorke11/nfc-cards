info:
  name: Get My Profile
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/profiles/me"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get my digital profile
