info:
  name: Delete Profile
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{baseUrl}}/profiles/me"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Delete my profile
