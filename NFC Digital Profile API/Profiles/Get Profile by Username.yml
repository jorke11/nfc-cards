info:
  name: Get Profile by Username
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/profiles/username/{{username}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get public profile by username
