info:
  name: NFC Redirect
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/nfc/{{uniqueId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Redirect from NFC card scan to profile page
