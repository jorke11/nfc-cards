info:
  name: Delete Account
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/users/me"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Delete current user account
