meta {
  name: Upload Company Logo
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/storage/upload/company-logo
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:multipart-form {
  file: @file(/path/to/your/logo.png)
}
