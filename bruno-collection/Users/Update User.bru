meta {
  name: Update User
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}/users/{{userId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "email": "newemail@example.com"
  }
}
