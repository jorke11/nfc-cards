meta {
  name: Get Recent Views
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/analytics/profile/{{profileId}}/recent-views?limit=10
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:query {
  limit: 10
}
